// Generated by Ignite ignite.com/cli
import Module from 'cosmos-gaia-ts-client/cosmos.slashing.v1beta1/module'
import { unref } from 'vue'

import useGaia from '../use'

type SendMsgUnjailType = typeof Module.prototype.sendMsgUnjail

type QueryParamsType = typeof Module.prototype.queryParams
type QuerySigningInfoType = typeof Module.prototype.querySigningInfo
type QuerySigningInfosType = typeof Module.prototype.querySigningInfos

type Response = {
  sendMsgUnjail: SendMsgUnjailType

  queryParams: QueryParamsType
  querySigningInfo: QuerySigningInfoType
  querySigningInfos: QuerySigningInfosType
}

function useModule(): Response {
  let { gaia } = useGaia()

  let {
    sendMsgUnjail,

    queryParams,

    querySigningInfo,

    querySigningInfos
  } = unref(gaia.cosmosSlashingV1Beta1)

  sendMsgUnjail = sendMsgUnjail.bind(gaia.cosmosSlashingV1Beta1)

  queryParams = queryParams.bind(gaia.cosmosSlashingV1Beta1)

  querySigningInfo = querySigningInfo.bind(gaia.cosmosSlashingV1Beta1)

  querySigningInfos = querySigningInfos.bind(gaia.cosmosSlashingV1Beta1)

  return {
    sendMsgUnjail,

    queryParams,

    querySigningInfo,

    querySigningInfos
  }
}

export { useModule }

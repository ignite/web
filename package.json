{
  "name": "root",
  "private": true,
  "description": "A monorepo of Vue and JS packages for Cosmos SDK apps",
  "author": "Tendermint, Inc <hello@tendermint.com>",
  "homepage": "https://github.com/tendermint/vue",
  "license": "Apache-2.0",
  "licenses": [
    {
      "type": "Apache-2.0",
      "url": "http://www.apache.org/licenses/LICENSE-2.0"
    }
  ],
  "scripts": {
    "purge": "lerna exec -- rm -rf node_modules",
    "prebuild": "lerna  bootstrap",
    "build": "lerna  run build --scope='{@ignt/vue-library,@ignt/react-library}' --include-dependencies",
    "publish": "lerna publish --force-publish --canary --preid alpha-$(git rev-parse HEAD) --dist-tag alpha --yes --no-verify-access"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ignite/web.git"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
